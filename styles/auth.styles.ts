import { StyleSheet } from 'react-native';

export const authStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#fff',
    },
    btnContainer: {
        marginTop: 20,
        width: '80%',
    },
    text: {
        fontSize: 18,
        marginBottom: 20,
        fontWeight: 'bold',
        color: '#333',
    }
});